const balance = require('./balance');
const daily = require('./daily');
const leaderboard = require('./leaderboard');
const pay = require('./pay');
const adminMoney = require('./admin-money');

module.exports = [
  balance,
  daily,
  leaderboard,
  pay,
  adminMoney
];
